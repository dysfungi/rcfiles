---
name: my-commenting
description: >-
  Code commenting conventions. Use when writing or reviewing comments and
  inline documentation: comment to explain WHY (decisions, references, non-obvious
  context), never to restate what the code already says; document non-obvious design and
  architectural decisions at the source so future readers don't reverse-engineer them
  from git history. Keywords: comment, comments, commenting, docstring, inline docs,
  rationale, why.
---

# Commenting

- Comments are code: write them only to explain a decision, capture non-obvious context,
  or cite a reference — not to restate what the code already says.
- Keep task, ticket, PR, review-discussion, or session-specific narrative out of comments —
  describe the code as it exists now (durable references like specs/RFCs/design docs are still
  fine); that process history belongs in the appropriate record (see `my-audit-trail`), not the
  source.
- Document non-obvious design/architectural decisions at the source (docstring, comment,
  or adjacent README): the WHY, so a future reader understands it without digging through
  git history.
- No commented-out or dead code — delete it; version control remembers.
