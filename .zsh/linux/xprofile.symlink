if [[ "$GDB_SESSION" = xmonad ]] || [[ "$XDG_SESSION_DESKTOP" = xmonad ]]; then
    xrandr --output DP-2 --off --output HDMI-1 --off
    xrandr --output DVI-I-1 --primary --auto --rotate right \
        --output HDMI-1 --auto --right-of DVI-I-1 \
        --output DP-2 --auto --right-of HDMI-1
fi
