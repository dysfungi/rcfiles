# JSON Schema for .chezmoidata/p4-sync-excludes.yaml
# Validated via pre-commit hook: check-jsonschema
$schema: https://json-schema.org/draft/2020-12/schema

type: object
additionalProperties: false
required: [unreal_binary_assets]
properties:
  unreal_binary_assets:
    type: array
    minItems: 1
    uniqueItems: true
    items:
      type: string
      pattern: "^[a-z0-9]+$"
